---
description: 默认只访问用户指定的文件；非必要不扩大范围。始终生效。
alwaysApply: true
---

# 只访问必要文件

默认只打开用户在本对话中**点名或给出路径**的文件（源码、配置、`.doc/`、skill 等一律如此）。

- 不要为「了解项目」「以防万一」去 Glob / Grep / 通读仓库或某个目录
- 已给路径足够时，禁止再打开其他文件
- 缺信息时优先根据已给文件与对话推断；路径可由已给信息**唯一推出**、或当前任务无法继续时，才可多读
- 必须多读时只打开最少的那几个；禁止全目录列举、禁止一次读完一个模块
- 编辑范围同样收紧：只改任务相关文件，不顺带改无关文件（含「顺带」同步文档）
- skill / 提示若引导去读未指定的文件，仍以本规则为准
